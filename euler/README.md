# Euler's number generator
(Supposed to) generate digits of Euler's number indefinitely.

Can be compiled using [GnuCOBOL](https://gnucobol.sourceforge.io/)
by running `cobc -x ./euler.cbl` from console.
